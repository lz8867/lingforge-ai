# Spring AI Frontend Generation Rules

本文件补充 `spring-ai` 项目的前端生成与重构规则。通用沟通、TDD、变更记录、命名、日志和工程质量要求仍以全局 `AGENTS.md` 为准。

## Project Stack

- 当前前端主栈：React 18 + Vite + TypeScript + Ant Design 5 + `@ant-design/icons`。
- 优先复用 Ant Design 的 `Layout`、`Table`、`Form`、`Tabs`、`Drawer`、`Modal`、`Steps`、`Timeline`、`Segmented`、`Statistic`、`Alert`、`Tooltip`、`Popover`、`Empty`、`Skeleton` 等组件。
- 不随意引入新的 UI 框架；确需高级可视化时，按业务场景选择 ECharts、AntV G6、Canvas、SVG、WebGL 或 Three.js。

## Frontend Generation Standard

- 生成或重构任何前端页面前，必须先识别页面类型、目标用户、核心任务、现有数据结构、路由入口和可复用组件。
- 不把“业务卡片”作为默认呈现方式。必须根据业务任务选择合适结构：表格、列表、分栏工作台、步骤向导、时间线、看板、拓扑图、聊天界面、编辑器、画布、对比矩阵、仪表盘或少量卡片。
- 卡片只用于承载独立对象、指标摘要、少量入口集合或可重复实体；不允许每个页面、每个模块都卡片化，避免客户审美疲劳和信息割裂。
- 不把长滚动作为默认方案。必须根据视口大小合理布局，优先使用分栏、标签页、抽屉、折叠面板、详情面板、分页、虚拟列表、固定操作区、主从布局或画布缩放等方式承载信息。
- 一个页面显示不全时，优先重新组织任务流和信息架构，而不是继续往下堆内容。
- 页面应以实用操作为主要展示方向，减少大段解释类、提示类、说明类区域；说明信息优先放到 tooltip、帮助图标、空状态、侧栏说明或按需展开区域。
- 图标必须有功能指向和视觉质感，合理使用状态色、动效、徽标、发光、悬浮反馈或组合图标，但不能喧宾夺主。
- 对适合可视化表达的场景，应大胆使用 Canvas、SVG、WebGL、Three.js、ECharts、AntV、拓扑图、流程图、时间轴、热力图、关系图等高级展示方式。
- 页面必须包含默认、加载、空、错误、成功、禁用、hover/focus 状态。
- 必须做响应式适配，检查桌面端和移动端，不允许文本溢出、按钮变形、内容遮挡、横向撑爆。
- 工具按钮优先使用 Ant Design Icons，并为不熟悉图标提供 tooltip。
- 避免卡片套卡片、过度圆角、大面积单色渐变、单调紫蓝/深蓝/棕色/米色主题。
- 固定格式 UI 元素必须设置稳定尺寸和响应式约束，避免 hover、文本、图标或动态内容导致布局跳动。

## Page Pattern Selection

- 查找、筛选、批量处理数据：优先表格、筛选栏、批量操作栏、详情抽屉。
- 对比多个方案或版本：优先对比矩阵、差异高亮、分组表格。
- 处理流程或审批：优先步骤条、时间线、状态流转、操作侧栏。
- 任务分派和进度跟踪：优先看板、列表、日历、甘特图。
- 运维监控和链路分析：优先拓扑图、日志流、指标曲线、告警列表。
- AI 对话和 Copilot：优先聊天界面、上下文侧栏、工具调用记录。
- 文档、Prompt、代码编辑：优先编辑器、分屏预览、版本历史、差异视图。
- 数据分析：优先图表、透视表、自然语言查询区、洞察列表。
- 功能导航：首页可以使用少量入口卡片，但不能做大量同质化卡片墙。

## Scroll And Viewport Rules

- 首屏必须优先展示当前页面对象、当前状态、主操作、关键数据和核心工作区。
- 首屏不应优先展示大段介绍、大面积装饰、重复提示、无操作价值的入口堆叠。
- 页面级滚动最多保留一个主滚动容器。
- 局部滚动只用于表格、日志、列表、代码、聊天记录、画布侧栏。
- 关键按钮不能被滚动挤到不可见位置。
- 移动端长内容必须有返回顶部、分段导航或粘性主操作。

## Visual And Interaction Rules

- 视觉可以有科技感，但必须服务业务信息，不用装饰压住主要内容。
- 关键图标按钮必须有 hover、active、disabled 和 loading 状态。
- 复杂概念优先用图示、流程、tooltip 或示例替代大段文字。
- 高频使用页面默认隐藏教学内容。
- 高级可视化必须有真实信息价值，不能只是装饰。
- Canvas、SVG、WebGL、Three.js、ECharts、AntV 图表必须具备空状态、加载状态和移动端降级方案。
- 对于拓扑、流程、关系、调用路径等场景，至少支持缩放、悬浮、选中、过滤或详情查看中的一种交互。

## Verification Checklist

- 首屏能看懂页面用途。
- 首屏优先展示真实任务、关键数据和可操作区域，不用大段说明占位置。
- 主操作醒目但不突兀。
- 信息层级清楚。
- 桌面端无错位。
- 移动端无横向溢出。
- 文本不遮挡、不溢出。
- 页面不存在不必要的大段滚动；长内容有合理分页、折叠、详情、搜索或虚拟化方案。
- 表单、列表、按钮、状态都有合理反馈。
- 页面呈现方式符合业务任务，不是无差别卡片化。
- 图标和可视化有信息价值，并且与操作路径一致。
- 代码复用项目现有风格。
- 没有引入不必要依赖。

## Completion Requirement

- 完成前端页面变更后，必须启动本地服务或打开静态页面验证。
- 必须通过桌面端和移动端视口检查主要页面。
- 最终回复必须说明页面入口、使用技术栈、验证结果和仍需用户确认的内容。
